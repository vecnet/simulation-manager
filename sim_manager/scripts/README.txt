This Python package contains the scripts that are run separately from the
Django project (i.e., scripts for submitting the simulations in a group,
or running a single simulation).  Therefore, the scripts and modules here
in this package must have no Django import statements.